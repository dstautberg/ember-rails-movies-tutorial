{{#if isEditing}}
<button {{action "save"}} class="btn btn-primary">Save</button>
<button {{action "cancel"}} class="btn btn-primary">Cancel</button>
{{else}}
<button {{action "edit"}} class="btn btn-primary">Edit</button>
{{/if}}
<h2>{{name}} ({{year}})</h2>
<div>{{description}}</div>
{{outlet}}
